{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Actor creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('actor') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
